package com.example.myapplication;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class Login_user extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_login_user); // Assure-toi que le nom du fichier XML est correct

        // Ici, tu peux ajouter du code pour gérer les interactions avec les vues si nécessaire.
        // Pour l'instant, nous ne faisons que définir le contenu de l'activité.
    }
}